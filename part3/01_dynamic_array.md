#### 数组

1. 读取操作 **O(1)**    **地址 = 基地址 + （索引 * 元素大小）**
2. 更新也是 **O(1)**
3. 插入 **O(n)**  为了给新元素腾出空间，从索引开始到数组末尾所有的元素都必须向后移动一个位置。如果有n个元素，平均需要移动n/2个元素
4. 删除同插入一样 **O(n)**  为了保证数组连续性，从索引开始到数组末尾所有的元素都必须向前移动一个位置

